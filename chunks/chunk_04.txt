ющий тип"; } if (highCount >= 2) return "Смешанный профиль"; return "Смешанный профиль"; } function getMainProfile(scales) { if (scales.fusion.level === "high" && scales.autonomy.level === "low" && (scales.closeness.level === "low" || scales.anxious.level === "high")) { return "Созависимый профиль"; } if (scales.disorganized.level === "high" || (scales.anxious.level === "high" && scales.avoidant.level === "high")) { return "Дезорганизованная близость"; } if (scales.closeness.level === "high" && scales.autonomy.level === "high" && scales.fusion.level === "low") { return scales.secure.level === "high" ? "Зрелая близость" : "Надежная близость"; } if ((scales.anxious.level === "high" && scales.avoidant.percent < 50) || (scales.fusion.level === "high" && scales.closeness.level === "high")) { return "Тревожная близость"; } if (scales.avoidant.level === "high" && scales.anxious.percent < 50) { return "Избегающая близость"; } if (scales.autonomy.level === "high" && scales.closeness.level === "low") { return "Самодостаточная дистанция"; } if (scales.secure.level === "high" && scales.anxious.percent < 50 && scales.avoidant.percent < 50 && scales.disorganized.percent < 50) { return "Надежная близость"; } return "Смешанный профиль"; } function getTopInfluences(scales) { const candidates = [ { label: `${scaleMeta.anxious[scales.anxious.level]} привязанности`, weight: scales.anxious.percent, priority: 10 }, { label: `${scaleMeta.fusion[scales.fusion.level]}`, weight: scales.fusion.percent, priority: 9 }, { label: `${scaleMeta.autonomy[scales.autonomy.level]}`, weight: Math.max(scales.autonomy.percent, 100 - scales.autonomy.percent), priority: 8 }, { label: `${scaleMeta.avoidant[scales.avoidant.level]} близости`, weight: scales.avoidant.percent, priority: 7 }, { label: `${scaleMeta.disorganized[scales.disorganized.level]}`, weight: scales.disorganized.percent, priority: 7 }, { label: `${scaleMeta.closeness[scales.closeness.level]}`, weight: Math.max(scales.closeness.percent, 100 - scales.closeness.percent), priority: 6 }, { label: `${scaleMeta.secure[scales.secure.level]} привязанности`, weight: Math.max(scales.secure.percent, 100 - scales.secure.percent) - 8, priority: 5 } ]; return candidates .sort((a, b) => (b.weight - a.weight) || (b.priority - a.priority)) .slice(0, 3) .map(item => item.label); } function getPersonalRecommendation(scales) { if (scales.anxious.level === "high" && scales.fusion.level === "high" && scales.autonomy.level === "low") { return "Вы склонны искать чувство безопасности через отношения. В периоды дистанции можете терять контакт с собственными потребностями и чрезмерно ориентироваться на реакцию партнера. Основная задача развития — укрепление автономии и способности сохранять внутреннюю устойчивость независимо от поведения другого человека."; } if (scales.disorganized.level === "high") { return "Сейчас особенно важно не требовать от себя мгновенной стабильности в близости. Основная задача — создавать предсказуемость, замечать моменты внутреннего переключения и учиться возвращаться к себе до резких решений в отношениях."; } if (scales.avoidant.level === "high" && scales.closeness.level === "low") { return "Вашей системе может быть привычнее защищаться дистанцией. Основная задача развития — не ломать независимость, а постепенно добавлять безопасную уязвимость: говорить о чувствах, просить о поддержке и проверять, что близость не всегда означает потерю свободы."; } if (scales.secure.level === "high" && scales.autonomy.level === "high" && scales.closeness.level === "high" && scales.fusion.level === "low") { return "Сейчас ваша карта выглядит устойчивой: близость, автономия и надежность поддерживают друг друга. Зона развития — сохранять этот баланс в стрессовые периоды и не откладывать сложные разговоры."; } if (scales.fusion.level === "high" && scales.closeness.level === "high") { return "В отношениях для вас много тепла и эмоциональной связи, но высокая близость может смешиваться с тревогой и слиянием. Основная задача — не уменьшать способность любить, а учиться оставаться собой внутри сильного контакта."; } return "Главная задача развития сейчас — укреплять эмоциональную устойчивость, яснее различать свои чувства и реакции партнера, а также постепенно выстраивать такой формат близости, где есть и контакт, и пространство для себя."; } function drawRadar(items) { const canvas = document.querySelector("#radar"); const ctx = canvas.getContext("2d"); const center = canvas.width / 2; const radius = 160; const angles = items.map((_, index) => -Math.PI / 2 + (Math.PI * 2 * index / items.length)); ctx.clearRect(0, 0, canvas.width, canvas.height); ctx.lineWidth = 1; ctx.font = "14px system-ui, sans-serif"; ctx.textAlign = "center"; ctx.textBaseline = "middle"; [25, 50, 75, 100].forEach(level => { const r = radius * level / 100; ctx.beginPath(); angles.forEach((angle, index) => { const x = center + Math.cos(angle) * r; const y = center + Math.sin(angle) * r; index ? ctx.lineTo(x, y) : ctx.moveTo(x, y); }); ctx.closePath(); ctx.strokeStyle = "rgba(111, 143, 122, .22)"; ctx.stroke(); }); angles.forEach((angle, index) => { const x = center + Math.cos(angle) * radius; const y = center + Math.sin(angle) * radius; ctx.beginPath(); ctx.moveTo(center, center); ctx.lineTo(x, y); ctx.strokeStyle = "rgba(111, 143, 122, .16)"; ctx.stroke(); const labelX = center + Math.cos(angle) * (radius + 54); const labelY = center + Math.sin(angle) * (radius + 44); ctx.fillStyle = "#516058"; ctx.fillText(items[index].shortLabel, labelX, labelY); ctx.fillStyle = "#b9798d"; ctx.fillText(`${items[index].percent}%`, labelX, labelY + 18); }); ctx.beginPath(); items.forEach((item, index) => { const r = radius * item.percent / 100; const x = center + Math.cos(angles[index]) * r; const y = center + Math.sin(angles[index]) * r; index ? ctx.lineTo(x, y) : ctx.moveTo(x, y); }); ctx.closePath(); ctx.fillStyle = "rgba(185, 121, 141, .28)"; ctx.fill(); ctx.strokeStyle = "#b9798d"; ctx.lineWidth = 3; ctx.stroke(); } function renderList(selector, items) { document.querySelector(selector).innerHTML = items.map(item => `<li>${item}</li>`).join(""); } function showResults() { if (answers.size < totalQuestions) { document.querySelector("#warning").textContent = "Ответьте на все вопросы, чтобы увидеть результат."; return; } const scores = { secure: sumGroup("secure"), anxious: sumGroup("anxious"), avoidant: sumGroup("avoidant"), disorganized: sumGroup("disorganized"), fusion: sumGroup("fusion"), autonomy: sumGroup("autonomy"), closeness: sumGroup("closeness") }; const scales = { secure: { score: scores.secure, percent: getAttachmentPercent(scores.secure), level: null }, anxious: { score: scores.anxious, percent: getAttachmentPercent(scores.anxious), level: null }, avoidant: { score: scores.avoidant, percent: getAttachmentPercent(scores.avoidant), level: null }, disorganized: { score: scores.disorganized, percent: getAttachmentPercent(scores.disorganized), level: null }, fusion: { score: scores.fusion, percent: getRelationshipPercent(scores.fusion), level: null }, autonomy: { score: scores.autonomy, percent: getRelationshipPercent(scores.autonomy), level: null }, closeness: { score: scores.closeness, percent: getRelationshipPercent(scores.closeness), level: null } }; ["secure", "anxious", "avoidant", "disorganized"].forEach(key => { scales[key].level = getLevelFromPercent(scales[key].percent); }); ["fusion", "autonomy", "closeness"].forEach(key => { scales[key].level = getRelationshipLevel(scales[key].score); }); const attachmentType = getAttachmentType(scales); const profileName = getMainProfile(scales); const result = profileTexts[profileName]; const topInfluences = getTopInfluences(scales); const radarItems = [ { key: "secure", shortLabel: "Надежн.", percent: scales.secure.percent }, { key: "anxious", shortLabel: "Тревога", percent: scales.anxious.percent }, { key: "avoidant", shortLabel: "Избег.", percent: scales.avoidant.percent }, { key: "disorganized", shortLabel: "Дезорг.", percent: scal