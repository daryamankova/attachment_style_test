es.disorganized.percent }, { key: "fusion", shortLabel: "Слияние", percent: scales.fusion.percent }, { key: "autonomy", shortLabel: "Автон.", percent: scales.autonomy.percent }, { key: "closeness", shortLabel: "Близость", percent: scales.closeness.percent } ]; document.querySelector("#resultProfileName").textContent = profileName; document.querySelector("#resultTitle").textContent = `${profileName} · ${attachmentType}`; document.querySelector("#resultText").innerHTML = result.text.map(paragraph => `<p>${paragraph}</p>`).join(""); renderList("#strengths", result.strengths); renderList("#difficulties", result.difficulties); renderList("#scenarios", result.scenarios); renderList("#recommendations", result.recommendations); renderList("#topInfluences", topInfluences); document.querySelector("#bars").innerHTML = radarItems.map(item => { const meta = scaleMeta[item.key]; const scale = scales[item.key]; return ` <div><div class="bar-label"><span>${meta.label}</span><span>${scale.score}/${meta.max} · ${scale.percent}%</span></div><div class="bar"><span style="width:${scale.percent}%"></span></div></div> `; }).join(""); document.querySelector("#scaleSummary").innerHTML = radarItems.map(item => { const meta = scaleMeta[item.key]; const scale = scales[item.key]; return ` <div class="scale-card"><strong>${meta.label}: ${meta[scale.level]}</strong><span>${meta.texts[scale.level]}</span></div> `; }).join(""); document.querySelector("#personalRecommendations").textContent = getPersonalRecommendation(scales); document.querySelector("#nextStep").innerHTML = ` <p>Сейчас наибольшее влияние на ваши отношения оказывают не только навыки общения и не техники знакомств, а особенности привязанности, самооценки и эмоциональной близости.</p><p>Именно работа с этими механизмами обычно помогает перестать повторять одни и те же сценарии в отношениях.</p> `; drawRadar(radarItems); document.querySelector("#results").classList.add("visible"); document.querySelector("#results").scrollIntoView({ behavior: "smooth", block: "start" }); } renderQuiz(); updateProgress(); quiz.addEventListener("click", event => { const button = event.target.closest(".answer"); if (!button) return; const id = button.dataset.id; const value = Number(button.dataset.value); answers.set(id, value); document.querySelectorAll(`.answer[data-id="${id}"]`).forEach(item => { item.classList.toggle("selected", item === button); item.setAttribute("aria-pressed", item === button ? "true" : "false"); }); updateProgress(); }); document.querySelector("#calculate").addEventListener("click", showResults); document.querySelector("#reset").addEventListener("click", () => { answers.clear(); document.querySelectorAll(".answer").forEach(button => { button.classList.remove("selected"); button.setAttribute("aria-pressed", "false"); }); document.querySelector("#results").classList.remove("visible"); updateProgress(); window.scrollTo({ top: document.querySelector("#test").offsetTop - 12, behavior: "smooth" }); }); document.querySelectorAll("[data-scroll]").forEach(button => { button.addEventListener("click", () => { document.querySelector(button.dataset.scroll).scrollIntoView({ behavior: "smooth", block: "start" }); }); }); (function(){window.dataLayer=window.dataLayer||[];window.gtag=function gtag(){window.dataLayer.push(arguments);};window.gtag("js",new Date());window.gtag("config","G-TKQYQ1CR4G");var s=document.createElement("script");s.async=true;s.src="https://www.googletagmanager.com/gtag/js?id=G-TKQYQ1CR4G";document.head.appendChild(s);})(); </script></body></html> 